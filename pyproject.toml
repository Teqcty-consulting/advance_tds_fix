[project]
name = "advance_tds_fix"
version = "0.1.0"
description = "Excludes GST from the TDS base on Payment Entries raised as advances against a Purchase Order"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.flit.module]
name = "advance_tds_fix"
